// import React from "react";
// import "@/assets/styles/components/ui/button.scss";

// type IProps = {
// 	className?: string;
// 	type?: "submit" | "reset" | "button" | undefined;
// 	disabled?: boolean;
// 	text: string;
// };

// const Button = ({ className, type, disabled, text }: IProps) => {
// 	return (
// 		<button className={`${className} button`} type={type} disabled={disabled}>
// 			{text}
// 		</button>
// 	);
// };

// export default Button;
