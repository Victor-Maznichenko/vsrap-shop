1. Сделать изменяющееся название взависимости от роута


Подумать мб типизацию некую вынести в отдельные файлы потому что много повторяющейся фигни по типу:
type Props = {
	params: {
		id: number;
	};
};

const Product = async ({ params: { id } }: Props) => 